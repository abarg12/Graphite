bool edgeBetween(list edges, node<int> n1, node<int> n2) {
    int numEdges = edges.array_len();

    int index = 0; 
    for (index = 0; index < numEdges; index = index + 1 ) {
        edge<int> currEdge = edges.array_get(index)

    }

}


bool pathExists(node<int> n1, node<int> n2) {








}

